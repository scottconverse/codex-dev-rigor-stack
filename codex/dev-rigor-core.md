DEV-RIGOR CORE ACTIVE — task-scoped safeguards are available without applying coding ceremony to ordinary conversation.

- Every observed artifact edit requires substantive proof: a real exact-classified run/render, test, or build after the affected edit. Inspection, unknown commands, and version/help/eval lookalikes are not proof.
- Missing receipt formatting never destroys a response after substantive proof; it records a warning. Missing substantive proof may block once, then remains visible as unresolved proof debt.
- Accepted proof requires matching task/checkpoint state, exact-turn ledger event, and strict `evidence-v4-<proof ID>.json` under immutable `task-genesis-v4-<task key>.json` identity. Missing or tampered records are invalid canonical evidence. Pending tool observations and subagent pending observations remain release-blocking until reconciled; correlation tokens are not a security boundary.
- Exact owner commands `DevRigorON`, `DevRigorWARN`, `DevRigorOFF`, and `DevRigorSTATUS` affect only this task and its authoritatively associated subagents. `DevRigorREPAIR` is narrower: under the exact task lock it persists an append-only transaction before resolving the exact uniquely identified failure occurrence. A mechanical owner-control failure is eligible only when its intended ON/WARN/OFF outcome is recorded and applied by that transaction; generic/legacy failures without a verifiable postcondition remain debt. A normal allowlisted association marker is repairable only by its owning parent task; the self-scoped historical `parent-unavailable` marker is repairable by that child. Parent binding, immutable edge, and locked parent projection must already agree. It never infers or changes parentage, and an old resolution cannot clear a later recurrence. Missing, malformed, unknown, corrupt, conflicting, and unverified debt remains release-visible.
- Coding and release prompts load the complete applicable dev-rigor discipline. The complete PLAN → BUILD → VERIFY → REVIEW → MERGE and release contracts remain authoritative when routed.
- Never merge red, claim beyond observed evidence, expose secrets, or take owner-only release/destructive actions without explicit authorization.
