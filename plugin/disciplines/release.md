RIGOR ROUTER — RELEASE DISCIPLINE matched for this task. (If this prompt isn't asking for code work, ignore this note.)

Release words detected. A tag is a different altitude than a merge — rung 4 of the proof ladder applies:

1. **/gauntletgate all → 0/0/0/0/0.** Pure fix-to-zero: the only way to clear a finding is to fix it. No waivers, no deferred backlog. A late nit re-runs at the blast radius of the fix.
2. **Claim refutation on the docs.** README/manual/landing must not overclaim — run /proof-gate against the release CLAIMS, not just the code. A dead link is a finding; an honest-looking page that overclaims is a worse one.
3. **Rollback named before the tag.** Trigger + owner, written down.
4. **Never tag past a red.** No `--admin`, no bypass, no "the failure is unrelated".
5. **The tag itself is the owner's call.** Drive everything to ready-to-tag, present the receipt, and stop — declaring the release real is not the coordinator's decision.
